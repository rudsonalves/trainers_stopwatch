import 'package:flutter/material.dart';

class AboutPage extends StatelessWidget {
  const AboutPage({super.key});

  static const routeName = '/abount';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
